<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">My Hommies App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if user_signed_in?%>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Menú
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <% if current_user.admin?%>
                <li>
                  <%= link_to 'Panel de control', admin_dashboard_path, class: 'dropdown-item'%>
                </li>
              <% end %>
              <li>
                <%= link_to 'Mi perfil', user_profile_path, class: 'dropdown-item'%>
              </li>
              <li>
                <%= link_to 'Cerrar sesión', destroy_user_session_path, method: :get, class: 'dropdown-item' %>
              </li>
        <% else %>
          <li class="nav-item mr-4">
            <%= link_to 'Registrate', new_user_registration_path, class: 'nav-link active' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Iniciar sesión', new_user_session_path, class: 'nav-link active' %>
          </li>
        <% end %>
  </div>
</nav>