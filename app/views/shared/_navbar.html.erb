<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="<%= root_path %>">My Hommies App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
      <% if user_signed_in?%>
        <li class="nav-item mr-4">
          <%= link_to 'Cerrar sesión', destroy_user_session_path, method: :get, class: 'nav_link' %>
        <li class="nav-item mr-4">
          <%= link_to 'Mi perfil', user_profile_path, class: 'nav_link'%>
          <% if current_user.admin?%>
            <li class="nav-item mr-4">
            <%= link_to 'Panel de control', admin_dashboard_path, class: 'nav_link'%>
          <% end %>
      <% else %>
        <li class="nav-item mr-4">
          <%= link_to 'Registrate', new_user_registration_path, class:'nav_link' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Iniciar sesión', new_user_session_path, class:'nav_link'%>
        </li>
      <% end %>
  </div>
</nav>