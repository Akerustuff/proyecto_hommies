<div class="container-fluid new-house-top h-100">
	<h2>Edita tu foto</h2>
  <div class="mb-5">
    <%= image_tag(@profile.edit_avatar_photo) %>
  </div>
  <%= form_for(@profile, :url => {:action => 'update', :id => @profile.id}, :html=> {:multipart => true }) do |f| %>
    <%= render 'form_photo', user: @profile %>
  <% end %>
      <%= link_to 'Regresar', user_profile_path, class:"text-decoration-none text-white mt-4" %>
</div>

<%= render "shared/footer" %>




